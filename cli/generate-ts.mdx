---
title: "generate-ts"
icon: "rectangle-terminal"
---

import DefinitionOption from "/snippets/cli-option-definition.mdx";
import OutFileOption from "/snippets/cli-option-out-file.mdx";
import TargetOption from "/snippets/cli-option-target.mdx";
import DebugOption from "/snippets/cli-option-debug.mdx";

Generates TypeScript type definitions for the specified schema and writes them to the specified file.

## Usage

```bash
typesync generate-ts --definition <filePathOrPattern> --target <targetEnvironment> --outFile <filePath> --indentation <indentation> --debug <debug>
```

## Options

<DefinitionOption />
<TargetOption />
<OutFileOption />

<ParamField type="int" path="indentation" default={2}>
  Indentation or tab width for the generated code.
</ParamField>

<DebugOption />

## Targets

- `firebase-admin@12`: For backend projects that rely on the [Firebase Admin Node.js SDK (v12)](https://www.npmjs.com/package/firebase-admin).
- `firebase-admin@11`: For backend projects that rely on the [Firebase Admin Node.js SDK (v11)](https://www.npmjs.com/package/firebase-admin).
- `firebase@10`: For frontend projects that rely on the [Firebase Javascript SDK (v10)](https://www.npmjs.com/package/firebase).
- `firebase@9`: For frontend projects that rely on the [Firebase Javascript SDK (v9)](https://www.npmjs.com/package/firebase).
